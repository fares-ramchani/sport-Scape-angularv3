import { SearchPropritairePipe } from './search-propritaire.pipe';

describe('SearchPropritairePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPropritairePipe();
    expect(pipe).toBeTruthy();
  });
});
